{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Achilles (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/achilles",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake-build"
        }
    ]
}