name: Build CI image

on:
  push:
    branches: [main]
    paths:
      - '.devcontainer/Dockerfile'
      - '.github/workflows/build-ci-image.yml'
  pull_request:
    paths:
      - '.devcontainer/Dockerfile'
      - '.github/workflows/build-ci-image.yml'
  pull_request_target:
    types: [closed]
    paths:
      - '.devcontainer/Dockerfile'
      - '.github/workflows/build-ci-image.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ericjgoossen/achilles-ci

jobs:
  build-and-push:
    if: github.event_name != 'pull_request_target'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write  # required to push to GHCR

    steps:
      - uses: actions/checkout@v5

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image tags
        id: tags
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "tags=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:pr-${{ github.event.pull_request.number }}" >> "$GITHUB_OUTPUT"
          else
            echo "tags=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  cleanup-pr-image:
    if: github.event_name == 'pull_request_target'
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Delete PR image tag
        uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          package: achilles-ci
          tags: pr-${{ github.event.pull_request.number }}
          token: ${{ secrets.GITHUB_TOKEN }}